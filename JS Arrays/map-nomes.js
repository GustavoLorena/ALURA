const nomes = ['ana julia','Caiu vinicius', 'BIA silva'];

// const nomesPadronizados = nomes.map((nome) => {
//     return nome.toUpperCase();
// });

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase());

console.log(nomesPadronizados);